<h1>SessionUser#new</h1>
<p>Find me in app/views/session_user/new.html.erb</p>


<h1>Vendor In</h1>

<% if flash[:error] %>
	<p><%= flash[:error] %></p>
<% end %>

<%= simple_form_for(:update_specials, :url => "/session_user/update_specials") do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :photo %>
  <%= f.button :submit %>


<% end %>
<ul>
<% @users_near.each do |x| %>
<li> <%= User.find(x.user_id).user_type %></li>
<% end%>
</ul>

  <%= image_tag "http://maps.google.com/maps/api/staticmap?size=450x300&sensor=false&zoom=16&markers=#{@vendor.locations.last.latitude}%2C#{@vendor.locations.last.longitude}" %>



